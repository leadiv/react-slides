import React, { Component, PropTypes } from 'react';

require('./app.scss');

class App extends Component {
    render() {
        return (
            <div>
                Hello this will be a simple slide presentation app when done.
            </div>
        );
    }
}

export default App;
